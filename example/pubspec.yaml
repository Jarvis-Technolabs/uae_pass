name: example
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  uae_pass:
    path: ../


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

