import 'package:flutter/widgets.dart';

class UaePassButton extends StatelessWidget {
  const UaePassButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
